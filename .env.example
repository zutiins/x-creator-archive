# 管理后台密码。请将星号替换为至少 8 位的强密码，不要提交真实密码。
ADMIN_PASSWORD=********

# 本地服务监听端口。通常可保持默认值。
PORT=39090

# 如后续同步脚本需要 X Cookie，请仅在本地 .env 中填写，严禁提交 GitHub。
X_AUTH_TOKEN=********
X_CT0=********

# 可选代理地址。请将星号替换为实际主机、端口和凭据。
# 示例格式：http://用户名:密码@代理主机:端口
PROXY_URL=http://********:********@********:****
